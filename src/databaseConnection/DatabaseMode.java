package databaseConnection;

/**
 * @author yoanmartin
 * Enumeration representing the database table corresponding to the protocol used
 */
public enum DatabaseMode {
	STORAGE_OPTIMAL, SERVER_OPTIMAL;
}
